apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sales-pvc
  labels:
    name: sales-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Mi
  storageClassName: local-storage
